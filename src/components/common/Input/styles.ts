import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inputContainer: {
    backgroundColor: '#fff',
    borderRadius: 20,
    width: '100%',
    padding: 12,
    marginBottom: 20,
  },
});
